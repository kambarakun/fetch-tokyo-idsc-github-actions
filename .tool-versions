# uv のバージョン固定。mise と CI の astral-sh/setup-uv (version-file) が読み、uv 本体は読まない。
# pyproject.toml の [tool.uv] required-version に置くと Dependabot 同梱の uv が起動時に落ち、
# uv エコシステムの更新が無音で止まるため、ここが唯一の置き場所 (issue #681)。
# setup-uv は uv 行の末尾コメントを解釈しないので、コメントは独立行に書くこと。
# Python は .python-version (3.11) から解決されるため uv のみ宣言する。
uv 0.11.24
